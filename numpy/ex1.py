#numpy
#numpy stands for number python or numerical python
#it is used to work on numbers
#in python we have lists to work with arrays but they are slow in process
#numpy array provides output  50 times faster than python list
#it was invented by Travis Oliphant
#to work with numpy arrays we have to import numpy module
import numpy

print(numpy.__version__)

#alias name
#we can give alias name to numpy
import numpy as np

print(np.__version__)